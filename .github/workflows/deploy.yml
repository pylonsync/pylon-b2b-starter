name: Deploy

on:
  push:
    branches: [main]

# Disabled by default — set the required secrets and remove this guard
# block to enable. Manual deploy via `vercel --prod` + `fly deploy`
# also works without CI.
jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI deploy disabled. See DEPLOY.md or remove this job."

  # backend:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: superfly/flyctl-actions/setup-flyctl@master
  #     - run: cd packages/db && flyctl deploy --remote-only
  #       env:
  #         FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
  #
  # marketing:
  #   runs-on: ubuntu-latest
  #   needs: [backend]
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: oven-sh/setup-bun@v2
  #     - run: bun install
  #     - run: cd apps/marketing && bunx vercel --prod --token=${{ secrets.VERCEL_TOKEN }}
  #
  # dashboard:
  #   runs-on: ubuntu-latest
  #   needs: [backend]
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: oven-sh/setup-bun@v2
  #     - run: bun install
  #     - run: cd apps/dashboard && bunx vercel --prod --token=${{ secrets.VERCEL_TOKEN }}
